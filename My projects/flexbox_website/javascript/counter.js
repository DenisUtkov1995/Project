window.addEventListener('click', function (event) {
    let counter;
    if ((event.target.dataser.action) === 'plus' || (event.target.dataset.action) === 'minus') {
        const counterWrapper = event.target.closet('.counter-wrapper');
    }
})